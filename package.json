{
  "name": "stepin2green-backend",
  "version": "2.0.0",
  "description": "Stepin2Green volunteer form backend — Cloudflare Worker + TiDB Cloud",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@tidbcloud/serverless": "^0.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
